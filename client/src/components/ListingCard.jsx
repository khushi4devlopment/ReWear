import {Link} from 'react-router-dom';import {serverURL} from '../services/api.js';export default function ListingCard({item}){const img=item.images?.[0]?serverURL+item.images[0]:null;return <div className="card h-100 listing-card shadow-sm"><div className="listing-img">{img?<img src={img} alt={item.title}/>:<div className="placeholder-img">♻</div>}</div><div className="card-body"><div className="d-flex justify-content-between"><span className="badge text-bg-light">{item.category}</span><span className="small fw-bold text-success">₹{item.estimatedValue}</span></div><h5 className="mt-2 mb-1">{item.title}</h5><p className="small text-muted mb-2">{item.brand} · Size {item.size} · {item.condition}</p><p className="small mb-3">📍 {item.location}</p><Link className="btn btn-success w-100" to={`/items/${item._id}`}>View item</Link></div></div>}
