import {useState} from 'react';import {api} from '../services/api.js';import {useAuth} from '../context/AuthContext.jsx';export default function Profile(){const{user,token,save}=useAuth();const[f,setF]=useState({name:user.name,phone:user.phone||'',location:user.location||'',bio:user.bio||''});const[msg,setMsg]=useState('');const[error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');try{const x=await api('/auth/profile',{method:'PUT',token,body:f});save({token,user:x.user});setMsg('Profile updated')}catch(e){setMsg('');setError(e.message)}};return <div className="container py-5"><div className="auth-card mx-auto"><h2>My profile</h2>{msg&&<div className="alert alert-success">{msg}</div>}{error&&<div className="alert alert-danger">{error}</div>}<form onSubmit={submit}>{Object.entries({name:'Name',phone:'Phone',location:'Location',bio:'Bio'}).map(([k,l])=><div className="mb-3" key={k}><label>{l}</label>{k==='bio'?<textarea className="form-control" value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>:<input className="form-control" value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>}</div>)}<button className="btn btn-success w-100">Save changes</button></form></div></div>}
