import {useEffect,useState} from 'react';import {api} from '../services/api.js';import {useAuth} from '../context/AuthContext.jsx';export default function Admin(){const{token}=useAuth();const[stats,setStats]=useState({}),[users,setUsers]=useState([]),[listings,setListings]=useState([]);const load=async()=>{const[a,b,c]=await Promise.all([api('/admin/stats',{token}),api('/admin/users',{token}),api('/admin/listings',{token})]);setStats(a);setUsers(b.items);setListings(c.items)};useEffect(()=>{load()},[]);const toggle=async id=>{await api('/admin/users/'+id+'/toggle',{method:'PATCH',token});load()};const hide=async id=>{await api('/admin/listings/'+id+'/hide',{method:'PATCH',token});load()};return <div className="container py-4"><h2>Admin panel</h2><div className="row g-3 mb-4">{[['Users',stats.users],['Active listings',stats.listings],['Swap requests',stats.swaps],['Successful swaps',stats.successful]].map(x=><div className="col-6 col-lg-3" key={x[0]}><div className="stat-card"><span>{x[0]}</span><strong>{x[1]??0}</strong></div></div>)}</div><div className="card shadow-sm mb-4"><div className="card-body"><h5>Users</h5><div className="table-responsive"><table className="table align-middle"><thead><tr><th>Name</th><th>Email</th><th>Location</th><th>Status</th><th/></tr></thead><tbody>{users.map(u=><tr key={u._id}><td>{u.name}</td><td>{u.email}</td><td>{u.location}</td><td>{u.isActive?'Active':'Inactive'}</td><td><button className="btn btn-sm btn-outline-dark" onClick={()=>toggle(u._id)}>{u.isActive?'Deactivate':'Activate'}</button></td></tr>)}</tbody></table></div></div></div><div className="card shadow-sm"><div className="card-body"><h5>Listings moderation</h5><div className="table-responsive"><table className="table"><thead><tr><th>Item</th><th>Owner</th><th>Status</th><th/></tr></thead><tbody>{listings.map(i=><tr key={i._id}><td>{i.title}</td><td>{i.owner?.name}</td><td>{i.status}</td><td>{i.status!=='hidden'&&<button className="btn btn-sm btn-outline-danger" onClick={()=>hide(i._id)}>Hide</button>}</td></tr>)}</tbody></table></div></div></div></div>}
