import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {api} from '../services/api.js';import {useAuth} from '../context/AuthContext.jsx';
export default function Auth({register=false}){const nav=useNavigate(),loc=useLocation(),{save}=useAuth();const[form,setForm]=useState({name:'',email:'',password:'',location:''});const[error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');try{const x=await api(register?'/auth/register':'/auth/login',{method:'POST',body:form});save(x);nav(loc.state?.from||'/dashboard')}catch(e){setError(e.message)}};return <div className="container py-5"><div className="auth-card mx-auto"><h2>{register?'Create your ReWear account':'Welcome back'}</h2><p className="text-muted">{register?'Start exchanging instead of buying new.':'Continue your sustainable swaps.'}</p>{error&&<div className="alert alert-danger">{error}</div>}<form onSubmit={submit}>{register&&<><label>Name</label><input required className="form-control mb-3" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><label>Location</label><input required className="form-control mb-3" placeholder="Bareilly, Uttar Pradesh" value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/></>}<label>Email</label><input required type="email" className="form-control mb-3" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><label>Password</label><input required minLength="6" type="password" className="form-control mb-3" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn btn-success w-100">{register?'Create account':'Login'}</button></form><p className="text-center mt-3 mb-0">{register?'Already have an account? ':'New here? '}<Link to={register?'/login':'/register'}>{register?'Login':'Register'}</Link></p></div></div>}
